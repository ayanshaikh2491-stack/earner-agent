[project]
name = "agent-earner"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "flask>=3.1.3",
    "httpx>=0.28.1",
    "pyyaml>=6.0.3",
    "qrcode>=8.2",
    "rich>=15.0.0",
]
